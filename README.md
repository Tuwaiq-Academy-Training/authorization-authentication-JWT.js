
محتوى الملف :

- ماهي authentication  (المصادقة)
- ماهي Authorization (التفويض  او التخويل )
- انشاء  authentication في  middleware 
- ماهو JWT
- متى  نستخدم JSON Web Tokens
- ما هي بنية JSON Web Token
-  تفصيل اجزاء Json Web Token
- ا Putting all together
-  لماذا يجب علينا استخدام JSON Web Tokens


## 

يتم استخدام المصادقة والتفويض في الأمان ، لا سيما عندما يتعلق الأمر بالوصول إلى النظام. ومع ذلك ، هناك فرق كبير بين الحصول على الدخول إلى المنزل (المصادقة) وما يمكنك القيام به في الداخل (الترخيص).


## ماهي authentication  (المصادقة)

#### المصادقة هي عملية التحقق من هوية المستخدم من خلال الحصول على بيانات الاعتماد واستخدام بيانات الاعتماد هذه لتأكيد هوية المستخدم. تبدأ عملية التفويض إذا كانت بيانات الاعتماد شرعية. تتبع عملية التفويض دائمًا إجراء المصادقة.

#### لقد كنت بالفعل على علم بعملية المصادقة لأننا نقوم بها جميعًا يوميًا ، سواء في العمل (تسجيل الدخول إلى جهاز الكمبيوتر الخاص بك) أو في المنزل (تسجيل الدخول إلى موقع ويب). ومع ذلك ، فإن الحقيقة هي أن معظم "الأشياء" المتصلة بالإنترنت تتطلب منك إثبات هويتك من خلال تقديم بيانات الاعتماد.


##  ماهي Authorization (التفويض  او التخويل )


التخويل هو عملية السماح للمستخدمين المصادق عليهم بالوصول إلى الموارد عن طريق تحديد ما إذا كان لديهم أذونات الوصول إلى النظام. من خلال منح أو رفض تراخيص معينة لمستخدم مصادق عليه ، يمكّنك التفويض من التحكم في امتيازات الوصول.

لذلك ، يحدث التفويض بعد مصادقة النظام على هويتك ، مما يمنحك وصولاً كاملاً إلى الموارد مثل المعلومات والملفات وقواعد البيانات والأموال والأماكن وأي شيء آخر. ومع ذلك ، يؤثر التفويض على قدرتك على الوصول إلى النظام ومدى إمكانية القيام بذلك.

## انشاء authentication في middleware

يمكننا إنشاء مستخدم وتسجيل الدخول بنجاح. ومع ذلك ، سننشئ مسارًا يتطلب رمزًا مميزًا للمستخدم في الرأس ، وهو رمز JWT الذي أنشأناه سابقًا.

#### أضف الملف التالي داخل auth.js.

ال middleware/auth.js

        const jwt = require("jsonwebtoken");

        const config = process.env;

        const verifyToken = (req, res, next) => {
          const token =
            req.body.token || req.query.token || req.headers["x-access-token"];

          if (!token) {
            return res.status(403).send("A token is required for authentication");
          }
          try {
            const decoded = jwt.verify(token, config.TOKEN_KEY);
            req.user = decoded;
          } catch (err) {
            return res.status(401).send("Invalid Token");
          }
          return next();
        };

        module.exports = verifyToken;


#### فلنقم الآن بإنشاء المسار /welcome وتحديث app.js بالملف التالي لاختبار البرنامج middleware.

         const auth = require("./middleware/auth");

         app.post("/welcome", auth, (req, res) => {
           res.status(200).send("Welcome 🙌 ");
         });
         
         
شاهد النتيجة أدناه عندما نحاول الوصول إلى المسار /welcome الذي أنشأناه للتو دون تمرير رمز مميز في الرأس باستخدام مفتاح x-access-token.



![image](https://www.section.io/engineering-education/how-to-build-authentication-api-with-jwt-token-in-nodejs/error-response.png)


#### يمكننا الآن إضافة رمز مميز في header  باستخدام المفتاح x-access-token وإعادة الاختبار.

![image](https://www.section.io/engineering-education/how-to-build-authentication-api-with-jwt-token-in-nodejs/success-response.png)




##  ماهو  JWT

ال JSON Web Token (JWT) هو معيار مفتوح (RFC 7519) يحدد طريقة مدمجة وقائمة بذاتها لنقل المعلومات بشكل آمن بين الأطراف ككائن JSON. يمكن التحقق من هذه المعلومات والوثوق بها لأنها موقعة رقمياً. يمكن توقيع JWTs باستخدام سر (مع خوارزمية HMAC) أو زوج مفاتيح عام / خاص باستخدام RSA أو ECDSA.

على الرغم من أنه يمكن تشفير JWTs لتوفير السرية أيضًا بين الأطراف ، فإننا سنركز على الرموز المميزة الموقعة. يمكن أن تتحقق الرموز المميزة الموقعة من تكامل المطالبات الواردة بداخلها ، بينما تخفي الرموز المميزة المشفرة تلك المطالبات من الأطراف الأخرى. عندما يتم توقيع الرموز المميزة باستخدام أزواج المفاتيح العامة / الخاصة ، فإن التوقيع يشهد أيضًا أن الطرف الذي يحمل المفتاح الخاص هو الوحيد الذي قام بالتوقيع عليه.

## متى  نستخدم JSON Web Tokens؟

#### فيما يلي بعض السيناريوهات التي تكون فيها رموز ويب JSON مفيدة:


####  ا Authorization:
هذا هو السيناريو الأكثر شيوعًا لاستخدام JWT. بمجرد تسجيل المستخدم الدخول ، سيتضمن كل طلب لاحق JWT ، مما يسمح للمستخدم بالوصول إلى المسارات والخدمات والموارد المسموح بها باستخدام هذا الرمز المميز. تعد ميزة الدخول الأحادي ميزة تستخدم على نطاق واسع في الوقت الحاضر ، نظرًا لصغر حجمها وقدرتها على استخدامها بسهولة عبر المجالات المختلفة.

#### ا Information Exchange:
تُعد رموز الويب JSON طريقة جيدة لنقل المعلومات بين الأطراف بشكل آمن. نظرًا لأنه يمكن توقيع JWTs - على سبيل المثال ، باستخدام أزواج المفاتيح العامة / الخاصة - يمكنك التأكد من هوية المرسلين كما يقولون. بالإضافة إلى ذلك ، نظرًا لأنه يتم حساب التوقيع باستخدام الرأس والحمولة ، يمكنك أيضًا التحقق من عدم العبث بالمحتوى.


## ما هي بنية JSON Web Token؟

#### تتكون JSON Web Tokens في شكلها المضغوط من ثلاثة أجزاء مفصولة بنقاط (.) ، وهي:

- ا Header
- ا Payload
- ا Signature

لذلك ، يبدو نموذج JWT كما يلي.

                                                                                                                 xxxxx.yyyyy.zzzzz
                                                                                                                 
                                                                                                                 

## تفصيل اجزاء Json Web Token :

#### ال Header

يتكون الرأس عادةً من جزأين:

`نوع الرمز المميز ، وهو JWT ، `

`وخوارزمية التوقيع المستخدمة ، مثل HMAC SHA256 أو RSA.`

#### فمثلا 

            {
              "alg": "HS256",
              "typ": "JWT"
            }
بعد ذلك ، يكون JSON هذا مشفرًا باستخدام Base64Url ليشكل الجزء الأول من JWT.


#### ال Payload

الجزء الثاني من الرمز المميز هو الحمولة النافعة ، التي تحتوي على المطالبات. المطالبات هي بيانات حول الكيان (عادة ، المستخدم) وبيانات إضافية. هناك ثلاثة 
أنواع من المطالبات

1-ا Registered

2- ا public

3- ا private claims


- ا Registered claims : 
هذه مجموعة من المطالبات المحددة مسبقًا والتي ليست إلزامية ولكنها موصى بها لتقديم مجموعة من المطالبات المفيدة والقابلة للتشغيل المتبادل. بعضها: المصدر (المُصدر) ، الصلاحية (وقت انتهاء الصلاحية) ، الفرع (الموضوع) ، aud (الجمهور) ، وغيرها.

                                لاحظ أن أسماء المطالبات تتكون من ثلاثة أحرف فقط طالما أن JWT من المفترض أن تكون مضغوطة.
                                
 
 - ا Public claims :
 يمكن تعريفها حسب الرغبة من قبل أولئك الذين يستخدمون JWTs. ولكن لتجنب الاصطدامات ، يجب تحديدها في IANA JSON Web Token Registry أو تعريفها على أنها URI الذي يحتوي على مساحة اسم مقاومة للتصادم.
 
 - ا Private claims:
 هذه هي المطالبات المخصصة التي تم إنشاؤها لمشاركة المعلومات بين الأطراف التي توافق على استخدامها وليست مطالبات مسجلة أو عامة.
 
 #### مثال على payload يمكن ان يكون 
 
        {
         "sub": "1234567890",
         "name": "John Doe",
         "admin": true
       }
       
يتم بعد ذلك ترميز payload  باستخدام Base64Url لتشكيل الجزء الثاني من JSON Web Token.


لاحظ أنه بالنسبة للرموز الموقعة ، يمكن لأي شخص قراءة هذه المعلومات ، على الرغم من حمايتها ضد التلاعب. لا تضع معلومات سرية في payload  أو header elements لـ JWT ما لم تكن مشفرة.



- ا Signature :
لإنشاء جزء signature  ، يجب أن تأخذ  header ، payload المشفرة ، والسر ، والخوارزمية المحددة في header ، وتوقيع ذلك.


#### على سبيل المثال ، إذا كنت تريد استخدام خوارزمية HMAC SHA256 ، فسيتم إنشاء التوقيع بالطريقة التالية:

      HMACSHA256(
        base64UrlEncode(header) + "." +
        base64UrlEncode(payload),
        secret)
        
        
يتم استخدام التوقيع للتحقق من عدم تغيير الرسالة على طول الطريق ، وفي حالة الرموز المميزة الموقعة بمفتاح خاص ، يمكن أيضًا التحقق من أن مرسل JWT هو الذي يقوله.


#### ا Putting all together

الناتج هو ثلاث سلاسل Base64-URL مفصولة بنقاط يمكن تمريرها بسهولة في بيئات HTML و HTTP ، بينما تكون أكثر إحكاما عند مقارنتها بالمعايير المستندة إلى XML مثل SAML.

يعرض ما يلي JWT الذي يحتوي على header  payload  السابقة المشفرة ، ويتم توقيعه بسر.


![image](https://cdn.auth0.com/content/jwt/encoded-jwt3.png)

إذا كنت استخدام JWT ووضع هذه المفاهيم موضع التنفيذ ، فيمكنك استخدام jwt.io Debugger لفك تشفير JWTs والتحقق منها وإنشائها.

![image](https://cdn.auth0.com/blog/legacy-app-auth/legacy-app-auth-5.png)

## كيف تعمل JSON Web Tokens؟

في المصادقة ، عندما يقوم المستخدم بتسجيل الدخول بنجاح باستخدام بيانات الاعتماد الخاصة به ، سيتم إرجاع JSON Web Token. نظرًا لأن الرموز المميزة عبارة عن بيانات اعتماد ، يجب توخي الحذر الشديد لمنع حدوث مشكلات أمنية. بشكل عام ، يجب ألا تحتفظ بالرموز لمدة أطول من المطلوب.


عندما يريد المستخدم الوصول إلى مسار أو مورد محمي ، يجب على وكيل المستخدم إرسال JWT ، عادةً في Authorization header باستخدام  Bearer schema. يجب أن يبدو محتوى العنوان كما يلي:


                                                Authorization: Bearer <token>
                                                
                                                
 
يمكن أن يكون هذا ، في بعض الحالات ، آلية ترخيص عديمي الجنسية. ستتحقق المسارات المحمية للخادم من وجود  Authorization  JWT   في header ، وإذا كان موجودًا ، فسيُسمح للمستخدم بالوصول إلى الموارد المحمية. إذا احتوت JWT على البيانات الضرورية ، فقد تقل الحاجة إلى الاستعلام عن قاعدة البيانات لعمليات معينة ، على الرغم من أن هذا قد لا يكون هو الحال دائمًا.
 
 
 إذا تم إرسال الرمز المميز فيAuthorization header,  ، فلن تكون مشاركة الموارد عبر الأصول (CORS) مشكلة لأنها لا تستخدم ملفات تعريف الارتباط.
 
 
 
 
 #### يوضح الرسم التخطيطي التالي كيف يتم الحصول على JWT واستخدامه للوصول إلى واجهات برمجة التطبيقات أو الموارد:
 
 ![image](https://cdn2.auth0.com/docs/media/articles/api-auth/client-credentials-grant.png)


- لاحظ أنه باستخدام الرموز المميزة الموقعة ، يتم عرض جميع المعلومات الموجودة في الرمز المميز للمستخدمين أو الأطراف الأخرى ، على الرغم من أنهم غير قادرين على تغييرها. هذا يعني أنه لا يجب وضع معلومات سرية داخل الرمز المميز.



## لماذا يجب علينا استخدام JSON Web Tokens؟

- لنتحدث عن فوائد JSON Web Tokens (JWT) عند مقارنتها برموز الويب البسيطة (SWT) ورموز لغة ترميز تأكيد الأمان (SAML).

- نظرًا لأن JSON أقل طولًا من XML ، فعند تشفيرها يكون حجمها أصغر أيضًا ، مما يجعل JWT أكثر ضغطًا من SAML. هذا يجعل JWT خيارًا جيدًا ليتم تمريره في بيئات HTML و HTTP.

- من الناحية الأمنية ، لا يمكن توقيع SWT إلا بشكل متماثل بواسطة سر مشترك باستخدام خوارزمية HMAC. ومع ذلك ، يمكن لرموز JWT و SAML استخدام زوج مفاتيح عام / خاص في شكل شهادة X.509 للتوقيع. يعد توقيع XML باستخدام XML Digital Signature دون إدخال ثغرات أمنية غامضة أمرًا صعبًا للغاية عند مقارنته بساطة توقيع JSON.

- تعد محللات JSON شائعة في معظم لغات البرمجة لأنها ترتبط مباشرة بالكائنات. على العكس من ذلك ، لا يحتوي XML على تخطيط طبيعي من مستند إلى كائن. هذا يجعل العمل مع JWT أسهل من استخدام تأكيدات SAML.


فيما يتعلق بالاستخدام ، يتم استخدام JWT على نطاق الإنترنت. يسلط هذا الضوء على سهولة معالجة جانب العميل لرمز ويب JSON على منصات متعددة ، وخاصة الأجهزة المحمولة.

![image](https://cdn.auth0.com/content/jwt/comparing-jwt-vs-saml2.png)

#### مقارنة بين طول JWT المشفر و SAML المشفر
